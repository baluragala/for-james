//es6

let course = {
    title: 'Angular',
    desc: 'by google',
    fee: 100
};

let courseAsString = 'Title : ' + course.title + ', Desc: ' + course.desc;
let courseAsString1 = `
                Title : ${course.title} , 
                Desc: ${course.desc}
`;
